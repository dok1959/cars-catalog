<form method="post" action="{{ path('admin_delete', {'id': user.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить запись?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-primary">Удалить</button>
</form>