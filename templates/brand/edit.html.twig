{% extends 'base.html.twig' %}

{% block title %}Редактировать марку{% endblock %}

{% block body %}
    <h2>Редактировать марку</h2>

    <div class="container">
        <div class="text-center">
            {{ include('brand/_form.html.twig', {'button_label': 'Обновить'}) }}

            <a class="btn btn-primary" href="{{ path('brand_index') }}">Вернуться</a>

            {% if is_granted('ROLE_ADMIN') %}
                {{ include('brand/_delete_form.html.twig') }}
            {% endif %}
        </div>
    </div>
{% endblock %}
