<form method="post" action="{{ path('brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить запись?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="btn">Удалить</button>
</form>
