<form method="post" action="{{ path('model_delete', {'id': model.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить запись?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ model.id) }}">
    <button class="btn">Удалить</button>
</form>