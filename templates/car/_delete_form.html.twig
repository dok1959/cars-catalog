<form method="post" action="{{ path('car_delete', {'id': car.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить запись?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.id) }}">
    <button class="btn btn-primary">Удалить</button>
</form>
